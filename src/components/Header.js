import React from 'react';

const Header = () => {
    return (
        <header style={{ backgroundColor: '#2d6cdf', padding: '10px', textAlign: 'center', color: '#fff' }}>
            <h1>PHR System</h1>
        </header>
    );
};

export default Header;
